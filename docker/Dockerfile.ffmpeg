FROM node:20-slim

RUN apt-get update && apt-get install -y ffmpeg && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY services/ffmpeg/package*.json ./
RUN npm install

COPY services/ffmpeg/ ./

EXPOSE 3002
CMD ["node", "server.js"]
